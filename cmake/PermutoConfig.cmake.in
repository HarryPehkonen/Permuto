# permuto/cmake/PermutoConfig.cmake.in

@PACKAGE_INIT@

# Set the library type for consumers
set(PERMUTO_SHARED @BUILD_SHARED_LIBS@)

# Find dependencies (nlohmann_json is header-only or linked into permuto-lib)
# find_dependency(nlohmann_json @nlohmann_json_VERSION@) # Adjust if needed

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/PermutoTargets.cmake")

# Check required components
check_required_components(permuto)

# Provide version information
set(PERMUTO_VERSION @PROJECT_VERSION@)
set(PERMUTO_VERSION_MAJOR @PROJECT_VERSION_MAJOR@)
set(PERMUTO_VERSION_MINOR @PROJECT_VERSION_MINOR@)
set(PERMUTO_VERSION_PATCH @PROJECT_VERSION_PATCH@)
